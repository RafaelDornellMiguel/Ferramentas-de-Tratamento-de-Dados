# Ferramentas de Tratamento de Dados (Streamlit)

App com 2 funcionalidades:
- 🧹 Limpar HTML/CSS + tratamento de espaços em Excel (.xlsx)
- 📦 Converter JSON → Excel/CSV

## Rodar local
```bash
pip install -r requirements.txt
streamlit run app.py
